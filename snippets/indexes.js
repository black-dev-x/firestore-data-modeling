// by default it creates an index for every property you have

// you can create indices manually (composite index)

// recommend letting firebase tell you when you need an index (the error will get you the link to create that specific index)
